# Screenshot Guide

Suggested screenshot directory:

```text
assets/screenshots/
```

## 1. Parse Demo 기본 화면

- App title: Sensory Atlas
- Subtitle visible
- Parse Demo tab selected
- Example selectbox and input area visible

Suggested file:

```text
assets/screenshots/01_parse_demo_default.png
```

## 2. Film-like 예시 결과

Input:

```text
해상도는 낮지만 분위기만 남아
```

Capture anchor `film_grain`, cue group `film_like_rendering`, and rendering `Film-like`.

Suggested file:

```text
assets/screenshots/02_film_like_result.png
```

## 3. Marble hall polish 예시 결과

Input:

```text
차가운 큰 홀의 매끈한 바닥처럼 고요하고 윤이 나
```

Capture anchor `marble`, light `Gloss`, texture `Smooth / Hard / Clean`, and cue group `marble_hall_polish`.

Suggested file:

```text
assets/screenshots/03_marble_hall_polish.png
```

## 4. Evaluation Dashboard

Capture dataset metrics, Top-1 / Top-3 charts, and holdout failure metrics.

Suggested file:

```text
assets/screenshots/04_evaluation_dashboard.png
```

## 5. Ontology Browser에서 film_grain 검색 결과

Search:

```text
film_grain
```

Capture object detail, core axes, and related/opposite objects.

Suggested file:

```text
assets/screenshots/05_ontology_film_grain.png
```
